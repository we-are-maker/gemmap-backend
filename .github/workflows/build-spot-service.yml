name: Build & Deploy Spot Service

on:
  push:
    branches:
      - develop
    paths:
      - 'services/spot-service/**'
      - 'libs/**'
      - '.github/workflows/build-spot-service.yml'
  pull_request:
    branches:
      - develop
    paths:
      - 'services/spot-service/**'
      - 'libs/**'
  workflow_dispatch:

jobs:
  build-test:
    name: Build & Test spot-service
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build spot-service (includes test)
        run: |
          ./gradlew :services:spot-service:clean :services:spot-service:build --no-daemon

  build-push-image:
    name: Build & Push spot-service Docker Image
    runs-on: ubuntu-latest
    needs: build-test
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    env:
      REGISTRY: docker.io
      IMAGE_NAME: gemmap/spot-service
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Short SHA
        id: meta
        shell: bash
        run: echo "sha7=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Docker Login
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image using Gradle bootBuildImage
        env:
          SHA7: ${{ steps.meta.outputs.sha7 }}
        run: |
          set -eux
          ./gradlew :services:spot-service:bootBuildImage \
            --imageName=$REGISTRY/$IMAGE_NAME:dev-$SHA7 \
            --no-daemon

      - name: Tag as dev-latest
        env:
          SHA7: ${{ steps.meta.outputs.sha7 }}
        run: |
          docker tag $REGISTRY/$IMAGE_NAME:dev-$SHA7 $REGISTRY/$IMAGE_NAME:dev-latest

      - name: Push Docker Images
        env:
          SHA7: ${{ steps.meta.outputs.sha7 }}
        run: |
          set -eux
          docker push $REGISTRY/$IMAGE_NAME:dev-$SHA7
          docker push $REGISTRY/$IMAGE_NAME:dev-latest

      - name: Execute NHN Pipeline - spot-service (dev)
        if: github.ref == 'refs/heads/develop'
        env:
          NHN_REGION: KR1
          NHN_APPKEY: ${{ secrets.NHN_APPKEY }}
          NHN_AUTH_ID: ${{ secrets.NHN_USER_ACCESS_KEY_ID }}
          NHN_AUTH_SECRET: ${{ secrets.NHN_USER_ACCESS_KEY_SECRET }}
          NHN_PIPELINE: contest11-dev-pipeline-2
        run: |
          set -eux
          curl --fail --show-error --location --max-time 20 -i -X POST \
            -H "Content-Type:application/json" \
            -H "X-NHN-REGION:${NHN_REGION}" \
            -H "X-TC-AUTHENTICATION-ID:${NHN_AUTH_ID}" \
            -H "X-TC-AUTHENTICATION-SECRET:${NHN_AUTH_SECRET}" \
            -H "X-NHN-APPKEY:${NHN_APPKEY}" \
            "https://kr1-pipeline.api.nhncloudservice.com/api/anchor/v1.0/pipelines/${NHN_PIPELINE}/execute"

      - name: Bump spot-service chart version
        if: success()
        run: |
          sed -i "s/^version: .*/version: 0.1.${GITHUB_RUN_NUMBER}/" charts/spot-service/Chart.yaml
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit -am "chore(spot-chart): bump 0.1.${GITHUB_RUN_NUMBER}" || true
          git push