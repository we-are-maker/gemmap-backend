apiVersion: v1
kind: Service
metadata:
  name: spot-service
  labels:
    app: spot-service
    service: spot
spec:
  type: ClusterIP
  selector:
    app: spot-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
